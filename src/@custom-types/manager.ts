export interface SignUpInput {
  availableTime: string;
  mobileNumber: string;
  status: string;
}

export interface SignUpOutput {
  availableTime: string;
  mobileNumber: string;
  status: string;
}
